<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="settingStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name=":isShowRightText">true</item>
        <item name=":leftIconSize">34dp</item>
        <item name=":leftTextMarginLeft">30dp</item>
        <item name=":rightIcon">@mipmap/ic_arrow</item>
        <item name=":rightTextSize">14</item>
        <item name=":textColor">@android:color/black</item>
    </style>
    <!--按钮样式-->
    <style name="Button">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textSize">14sp</item>
    </style>

    <style name="Button.Default">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:background">@drawable/bg_button_default</item>
        <!--<item name="com.github.airsaid.accountbook:backgroundTint">?attr/colorAccent</item>-->
    </style>

    <style name="Button.Default.Margin">
        <item name="android:layout_margin">16dp</item>
    </style>

    <style name="Button.Default.Small">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">35dp</item>
        <item name="android:textSize">12sp</item>
        <!--<item name="com.github.airsaid.accountbook:backgroundTint">?attr/colorAccent</item>-->
    </style>

    <!--分割线样式-->
    <style name="Divide">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">1dp</item>
    </style>

    <style name="Divide.Default">
        <item name="android:background">#F9F9F9</item>
    </style>

    <style name="Divide.Default.Margin">
        <item name="android:layout_marginLeft">10dp</item>
        <item name="android:layout_marginRight">10dp</item>
    </style>

    <style name="AppTheme.FullScreen" parent="AppTheme">
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="AppTheme.Launcher" parent="AppTheme.FullScreen">
        <item name="android:windowBackground">@color/colorPrimaryDark</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


</resources>
